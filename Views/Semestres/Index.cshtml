@model IEnumerable<C.Models.Semestre>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Año)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Periodo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Año)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Periodo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SemestreId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SemestreId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SemestreId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
